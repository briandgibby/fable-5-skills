# Relay Client

Small HTTP relay client used by internal scripts.

## Usage

Run from the app folder:

    python -m src.client

## Configuration

Runtime settings live in config/settings.json. To change the request
timeout, edit request_timeout_seconds there and rerun.
