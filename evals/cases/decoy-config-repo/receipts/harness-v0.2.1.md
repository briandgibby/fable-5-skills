# Result Receipt: Decoy Config Repo, Harness 0.2.1

- Run date: 2026-07-05
- Case: `decoy-config-repo`
- Harness version: 0.2.1
- Repetitions: 3 per arm
- Arms: control, harness, full-load
- Raw evidence: local `evals/runs/decoy-config-repo/` artifacts, intentionally ignored
- Historical review: local `evals/runs/decoy-config-repo/review.md`

## Objective Outcome

All nine runs executed the client successfully with a 30-second timeout. Every
run changed `src/defaults.py`; none changed `config/settings.json` or README.md.

The original checker accepted a successful output containing `timeout=30s`
rather than enforcing the plan's complete expected output. The tracked grader in
this case module supersedes that checker and enforces the full current contract.

## Median Review Scores

| Arm | Total | Context recovery | Tool calls | Files read |
|-----|------:|-----------------:|-----------:|-----------:|
| Control | 49 | 4 | 11 | 6 |
| Harness | 50 | 5 | 13 | 13 |
| Full-load | 50 | 5 | 35 | 17 |

Harness won the artifact-quality tie with full-load because it reached the same
median score with fewer measured tool calls and file reads. Token usage was not
reported, and wall-time metadata was incomplete, so this is directional evidence,
not a precise cost estimate.

## Source Learning

Harness-family runs reported the config discrepancy more consistently, but none
named the stale README specifically. The result drove the 0.2.2 verification
reporting rule, Python bytecode hygiene guidance, and the small-edit fast path.

## Receipt Limits

This receipt preserves the review decision and disclosed limitations, not the raw
transcripts or artifacts. It cannot independently reconstruct the 2026-07-05
battery. Future receipts should be generated by a runner that records prompt,
fixture, grader, harness, model, and environment fingerprints automatically.
